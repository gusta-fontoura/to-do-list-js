var y = 0; // variáveis são manipuladas

const PRECO = 2; // Constantes não são manipuladas

function soma(a,b) {  // declarando função
    return a + b  // não precisa necessáriamente do return. Ele é utilizando para que você consiga executar a função em outra função.
}

soma (3,5) // chamando a função você pode chamar funções sem parâmetros.

/* aprendendo a utilizar o console.
podemos utilizar o console em uma página da web ou em um terminal... 
Para rodar no Terminal é precisa usar o Node.JS*/

console.log("Hello World");